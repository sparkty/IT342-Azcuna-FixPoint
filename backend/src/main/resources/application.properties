# Server
server.port=8080

# Database (Supabase Pooler)
spring.datasource.url=jdbc:postgresql://aws-1-ap-southeast-2.pooler.supabase.com:5432/postgres
spring.datasource.username=postgres.vabxurenhebzdzavtasg
spring.datasource.password=fixpointabsolution727
spring.datasource.driver-class-name=org.postgresql.Driver

# Required for Supabase (SSL)
spring.datasource.hikari.data-source-properties.sslmode=require

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# (Optional but recommended: remove explicit dialect, Hibernate auto-detects it)
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JWT
jwt.secret=fixpoint_super_secret_key_must_be_at_least_256_bits_long_for_hs256
jwt.expiration=86400000
jwt.refresh-expiration=604800000

# CORS
app.cors.allowed-origins=http://localhost:5173